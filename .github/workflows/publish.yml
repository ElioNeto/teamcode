name: publish

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: "Override version"
        required: false
        type: string

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-24.04
    if: github.repository == 'ElioNeto/teamcode'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - uses: ./.github/actions/setup-bun
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Configure git user
        run: |
          git config --global user.name "teamcode-bot"
          git config --global user.email "teamcode-bot@users.noreply.github.com"

      # ── Version ──────────────────────────────────────────────
      - id: version
        run: |
          if [[ -n "${{ inputs.version }}" ]]; then
            echo "version=${{ inputs.version }}" >> $GITHUB_OUTPUT
          elif [[ "${{ github.ref_type }}" == "tag" ]]; then
            echo "version=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT
          else
            exit 1
          fi
          echo "tag=v${VERSION}" >> $GITHUB_OUTPUT
          echo "release=true" >> $GITHUB_OUTPUT
        env:
          VERSION: ${{ inputs.version || '' }}

      # ── Build CLI binary ───────────────────────────────────────
      - name: Build CLI
        run: ./packages/teamcode/script/build.ts
        env:
          TEAMCODE_VERSION: ${{ steps.version.outputs.version }}
          TEAMCODE_RELEASE: true
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}

      # ── Publish @teamcode-ai/* packages to npm ──────────────
      - name: Publish npm packages
        run: |
          ./script/publish.ts
        env:
          TEAMCODE_VERSION: ${{ steps.version.outputs.version }}
          TEAMCODE_RELEASE: true
          TEAMCODE_CHANNEL: latest
          GITHUB_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          BUN_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      # ── Publish VS Code extension ────────────────────────────
      - name: Publish VS Code extension
        continue-on-error: true
        working-directory: sdks/vscode
        run: |
          bun run package
          npx vsce publish --skip-duplicate
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
